# T11+T14+T16 — CI fail-safe, alerting stub, IndexNow (one workflow cluster)

Work ONLY on `dev`. Never touch `main`. **NEVER push — the reviewer pushes after verification;
your live-run proof obligations end at authoring valid workflows.**

## Build
1. **Full pipeline workflow** `.github/workflows/ci.yml`:
   - Trigger: every push to `dev` and every PR into `main`, EXCEPT when the ONLY changed paths
     are content/media (see bypass). Use `paths-ignore` or a filter job.
   - Jobs on ubuntu-latest, Node 22: install (npm ci), then the FULL gate battery as separate
     named steps: build, test:jsonld, test:t2, test:t4, test:t5, test:t6, test:t7, test:t8,
     test:t9, test:t10, test:t13, test:t15a, test:t17. Playwright browsers via
     `npx playwright install --with-deps chromium`. No continue-on-error anywhere.
2. **Bypass lane workflow** `.github/workflows/content.yml`: triggers ONLY when every changed
   path matches this pinned list: `src/content/**` or `src/assets/content/**` (create the empty
   dir with .gitkeep — the future home of authored content images; brand assets are NOT bypass
   paths); runs build + test:t2 (schema validation) ONLY. Ratified media bypass — validate-only.
3. **T14 alert stub**: in ci.yml, a final job `notify-failure` with `if: failure()` that invokes
   a webhook step reading `${{ secrets.ALERT_WEBHOOK_URL }}` and `${{ vars.ALERT_CHANNEL }}`;
   when unset (now), the step echoes "alert stub: no channel configured" and exits 0. Channel
   values are a Pete-supplied [FILL] — never invent a destination.
4. **T16 IndexNow**: generate a key file `public/<key>.txt` (a committed random hex key is fine —
   it is public by protocol design) and a `notify-indexnow` job in ci.yml gated
   `if: github.ref == 'refs/heads/main'` that pings the IndexNow endpoint — but since hosting is
   not ratified, the job body is a no-op echo stub with the real curl commented and labeled
   "enable at hosting ratification". The key file must ship in dist/ (verify the build copies
   it), AND the commented real curl must reference the SAME key filename and the SITE constant's
   host (test:t11 asserts the key filename appears in both public/ and the ci.yml curl line —
   the stub is a wired-but-dormant integration, not decoration).
5. **run-t10 hygiene fix**: in `tests/run-t10.sh`, export `TMPDIR=/tmp` (and `TEMP`/`TMP`) before
   `lhci autorun` — prevents mis-named temp dirs when Windows env leaks into WSL shells.
6. **Local validation** `tests/run-t11.sh` as `test:t11`: yaml-parses both workflows (node
   js-yaml or npx action-validator if installable), asserts: every test:* script in package.json
   (except itself) appears as a ci.yml step; content.yml triggers only on the bypass paths;
   notify-failure exists with if:failure(); indexnow job is main-gated; the key file exists in
   public/ and dist/ after build.

## Acceptance (yours)
1. `npm run test:t11` exits 0; full suite list + clean build still exit 0.
2. Commit `T11+T14+T16: CI pipeline, alert stub, IndexNow stub` with T11-14-16-CHECKPOINT.md and
   this file; status clean. DO NOT PUSH.

## Post-push proof (the reviewer's gate, recorded here for the traceability map)
After the reviewer pushes: `gh run list --branch=dev` shows ci.yml completed green with all
battery steps; a content-only test commit triggers content.yml alone; branch protection remains
API-verified. These are NOT your checks.
